function changePassword(event) {
  event.preventDefault();
  const oldPassword = document.getElementById("old");
  const newPassword = document.getElementById("new");

  if (oldPassword.value === w) {
  } else {
    alert("Old password is not same!");
  }
}
